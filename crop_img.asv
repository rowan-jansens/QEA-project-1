function img = crop_img(A, center, crop_size)

    c_frac_x = center(1);
    c_frac_y = 1 - center(2);

    [py, px] = size(A);
    
    
    
    x_pos = round(c_frac_x * px)
    y_pos = round(c_frac_y * py)
    
    
%     
%     a = round(x_pos - crop_size / 2);
%     b = round(x_pos + crop_size / 2)-1;
%     c = round(y_pos - crop_size / 2);
%     d = round(y_pos + crop_size / 2)-1;


    a = 
    
    img = A(a:b, c:d);

end